#include<stdio.h>  // Responsavel pelo comando printf
#include<conio.h>  // Responsavel pelo comando getch
#include<string.h> // Responsavel pelo comando strlen


main() {
	/* Preencher um vetor com 8 numeros inteiros; solicitar um n�mero do teclado. Pesquisar se esse numero existe no vetor. 
	Se existir, imprimir em qual posi��o do vetor e qual a ordem foi digitado. Se nao existir, imprimir MSG que nao existe. */
	
}
 

